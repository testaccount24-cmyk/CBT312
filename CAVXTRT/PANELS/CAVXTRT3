)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(yellow)
~ type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high)
)Body Expand(\\)
%-\-\- ~CA-View Report Database%-\-\-
%Command ===>_zcmd
+
~Select any single CA-View (aka SAR) Report Database with`point-and-shoot~or
~any`non-blank~character.  Then press`Enter~to start the CA-View Report
~Extract, View, Mail dialog.
+
~Use of selection option`C~will invoke the Computer Associated CA-View
~ISPF Dialog.
+
)Init
 &zup = cavxtrth
)End
