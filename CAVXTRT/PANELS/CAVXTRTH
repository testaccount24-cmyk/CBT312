)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(yellow)
~ type(text) intens(high) caps(off) just(asis ) color(turq) hilite(reverse)
# type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high) color(red)
@ type(output) intens(high) caps(on) just(left ) color(yellow)
)Body Expand(\\)
%-\-\- ~CA-View Extract Reports Tutorial @ver %-\-\-
%Selection ===>_ZCMD                                                           +
%
`  This ISPF Dialog provides a simple interface to the CA-View Report
`  archive database.  This dialog provides additional functionality to
`  the Computer Associates CA-View ISPF interface.
`
`  The following topics are presented in sequence, or may be selected by
`  number:
`
   %1+ Report Selection Criteria
   %2+ Report Selection Processing Options
   %3+ Report Database Selection
   %4+ Sorting Report Display Table
+
)Proc
     &zsel = trans( &zcmd
                1,cavxtrt0
                2,cavxtrt2
                3,cavxtrt3
                4,cavxtrt1
                *,'?'
                )
     &zup = cavxtrth
)end
