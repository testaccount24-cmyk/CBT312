)attr default(%+_)
` type(text) intens(high) caps(off) just(asis ) color(yellow)
~ type(text) intens(high) caps(off) just(asis ) color(turq)
% type(text) intens(high)
)Body Expand(\\)
%-\-\- ~CA-View Sort Report Table%-\-\-
%Command ===>_zcmd
+
   `Sort+the Report Table using any of the column headings except Sel and Act)
+
+  ~Syntax:`Sort column-heading+
+        or
+          `Sort column-heading,order+
+
+   order is either A for ascending or D for descending
+   * only 1 column may be sorted at a time
)Init
 &zup = cavxtrth
)End
