)Attr Default(%`_)
/* _ type( input) intens(high) caps(on ) just(left )               */
   ~ type(text) intens(high) caps(off) just(asis ) color(turq)
   % type(text) intens(high)
)Body  Expand(\\)
%-\-\- ~CSVEDIT Help% -\-\-
%Command ===>_zcmd
%
`  The CSVEDIT Tool is an ISPF Edit Command.
`
`  Either define the columns as the first line of data in your file using
`  a > symbol for the start of each column.
`
`  Or specify the column start locations on the CSVEDIT Panel.
`
`  Specify if leading zeros are to be preserved.
`
`  You can also specify an alternate character to replace the ,
`
`  Define the column and data value to be tested for records to be
`  ignored (records that match will be deleted).
`
`  NOTE: There must be available free space in the record to insert the
`        CSV changes.
)Init
  .cursor = zcmd
)Proc
)end
